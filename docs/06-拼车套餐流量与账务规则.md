> 当前实现说明（2026-09-16）：本文件保留设计目标与历史方案。已实现范围、测试证据和剩余差异以 [实现核对](reference/implementation-audit.json) 及三个仓库的 README 为准；未公开部分已获准采用 ASWired 原创等效实现，不承诺妙妙屋私有协议互通。

# 拼车、套餐、流量与账务规则

> 早期业务草案说明：本文仍保留旧版业务模型或原型交互供参考。自行提出的车主/多租户、AA账本、人工审批及付款流程不是本轮新增要求；角色、套餐和续费等实际行为以[第15份当前矩阵](15-完整功能与PRO对齐矩阵.md)及固定版本核对为准，不据旧稿另设流程。

> 当前决策（2026-09-15）：除PRO商业授权外，采用妙妙屋X的公开设计和固定版本行为，ASWired全部功能不设置付费墙。此前自拟的Agent多进程/mTLS、独立协调器、Vision拷贝替代、离线双策略及JWT刷新方案已撤回；当前节点、运行时、运维与登录规则以[13](13-子Agent详细设计.md)、[16](16-增强运行时与PRO执行设计.md)、[17](17-Agent运维测速与联邦设计.md)、[18](18-自有探针与登录回接设计.md)的0.3修订及[完整矩阵](15-完整功能与PRO对齐矩阵.md)为准。品牌、固定深色、三私有仓、JWT、自有探针和外部仅Komari1.2.5-fix2为用户既定要求。以下业务模型不是上游私有协议声明，尚未实现。


本章计量与账务独立于自有及 Komari 1.2.5-fix2 主机探针。两种来源的主机网卡流量只作运维参考，不生成成员消费、配额扣减或金额分录；探针断连不修改账本。以下 Agent 指 Xray 管理及计量 Agent。

## 5. 最后席位并发、支付去重与AA分账

### 5.1 暂留和分配伪代码

```text
reserveSeat(groupId, userId, idemKey):
  BEGIN
  if saved_result(idemKey): return saved_result
  lock group FOR UPDATE
  require group accepts joining
  release expired HELD slots under this group, preserve hold history
  slot = choose first FREE slot FOR UPDATE
  if none: fail SEAT_CAPACITY_EXHAUSTED
  hold = create ACTIVE hold(expiresAt = databaseNow + configuredTTL)
  update slot FREE → HELD only if current state is FREE
  create order with immutable price and policy snapshot
  save idempotent result and outbox event
  COMMIT

applyVerifiedPayment(event):
  verify provider signature/merchant/order/currency/amount outside business lock
  BEGIN
  insert payment_event(provider,eventId,payloadHash); duplicate same hash → no-op
  lock group, slot, order in canonical order
  if this payment success already applied: COMMIT and return
  append balanced monetary transaction and set payment SUCCEEDED
  if slot still HELD by this valid hold:
      create seat; slot → OCCUPIED; hold → CONSUMED
      create entitlement and quota grants, each unique by order line
      order → FULFILLING; outbox → entitlement.changed
  else if policy allows and FREE slot exists:
      allocate another FREE slot under the same locks; same grant flow
  else:
      order → PAID_UNALLOCATED; outbox → allocation.attention_required
  COMMIT
```

数据库时间决定hold是否过期。重试同幂等键但不同请求body返回409，不能悄悄创建另一个订单。付款回调ack丢失后重送不能再次记款或发额度。外部付款通知中的用户输入不能直接当套餐价格依据。

### 5.2 AA整数分账

每个账期先冻结参与者、权重、费用项、币种和规则版本。默认等权；按天加入/退出或特殊席位可指定整数权重（例如有效分钟数），但须在组策略中公开，不能结算时临时调整。

给定总额 `C ≥ 0`（分）、席位权重 `w[i] ≥ 0`，`W=Σw[i]>0`：

```text
base[i] = floor(C * w[i] / W)
remainder[i] = (C * w[i]) mod W
left = C - Σbase[i]
按 remainder降序、稳定seat_id升序排序
前left个席位各加1分
assert Σallocated[i] == C
```

例如100.00元三人等分：33.34、33.33、33.33。谁拿余数由冻结的稳定排序决定，预览和结算一致。乘法使用足够精度的整数/数据库numeric，不能先转浮点。负数调整先对绝对值分配再统一取负；跨币种禁止直接均摊，若需换汇，另存汇率来源、时点和明确舍入策略。

费用处理分开：已付费用、预估下期费用、应收AA、实际收款。总退款按原付款可退余额约束；组中途退出的按日退款是新计算快照和负调整分录，不能修改已经SETTLED的AA分配。欠款与代理停用的宽限期也要版本化。



## 9. 流量统计与恢复

采用上游公开的入站/用户累计计数器、非清零读取和增量上报，主控按其套餐/物理节点/路由子节点及独立用户凭据归属口径结算。主机网卡仅作观测，不重复进入成员消费。[内嵌统计说明](https://miaomiaowux.com/docs/embedded-xray/)、[流量统计口径](https://miaomiaowux.com/docs/traffic-accounting/)

上下行方向、单向或双向统计、倍率、周期、手动校准及未归属流量按固定版本样例记录。原生与3X-UI来源单独归一；同一事实不能在master汇总和节点明细中各记一次。

重启、计数器回退、旧库恢复、断连和重放必须逐场景核对上游；发布说明已提到相关修复，但不足以确认Agent内部WAL结构或全部丢账上界。主控可以记录归一化epoch和缺口以便审计，不能强制节点采用此前自拟样本信封、fsync/ACK水位协议。

## 10. 套餐额度、超额与离线

按妙妙屋公开设计实现额度、到期、超额停用/降速、恢复以及用户覆盖；连接数按用户＋物理节点共享，超出连接上限拒绝新连接，已有连接按对应操作语义处理。限速推送和不同更新触发点按固定版本核对。[套餐](https://miaomiaowux.com/docs/packages/)、[节点限速](https://miaomiaowux.com/docs/node-ratelimit/)

此前自拟METERED/BOUNDED/STRICT等级、绝对授权lease、全局字节保留/guard、离线availability/accounting及其超额公式不再作为当前方案。故障期间是否继续代理、重启能否用缓存、磁盘失败的动作与恢复顺序按锁定上游版本记录；未公开细节列为待核对，不额外增加上游没有的本地停服门槛。

商业许可证或授权槽位不参与ASWired额度执行，也不构成Agent运行依赖。账号/套餐权限、节点认证与真正的到期/超额业务规则照常生效。

## 11. 订阅、独立凭据和撤销

### 11.1 订阅入口

建议公共入口为 `GET /s/{opaqueToken}?format=auto|raw|mihomo|xray-json`，只在订阅域名提供；管理API与订阅域名分离。token对应一个个人主体或一个seat，无明文用户邮箱。显式format优先，auto按有限UA规则检测；UA不决定权益和权限。

网关处理：hash token→校验token和subject→读取最新权益/暂停水位→过滤已验证可交付节点→按客户端能力渲染→设置用量/到期头→审计脱敏下载事件。命中缓存前也要检查撤销水位；缓存key包含entitlement、credential、node-selection、renderer版本。公共反向代理访问日志对路径token掩码；不能用完整订阅URL作为监控标签。

订阅头中的 `upload/download/total` 为bytes，`expire`为Unix秒。个人池展示个人权益；共享组池可以展示“全车共享池剩余”，但需在信息页说明，不展示其他成员身份。过期/暂停不返回仍能连接的配置；原始客户端响应与人类信息页分开，禁止把管理系统HTML错误页当订阅正文。

### 11.2 凭据轮换与接入行为

订阅链接Token、用户代理凭据、节点Token的轮换与失效分别按固定上游版本核对。旧稿自行规定的prepare、snapshot、撤销顺序、默认宽限和REVOKE_PENDING协议已撤回；节点离线、旧连接、已下载配置和重试影响均需实际样本确认，不能把订阅链接失效推断为代理连接已经终止。

保留公开的连接数、在线IP及对应版本客户端约束。硬件标识和IP/连接数量是不同口径，准确支持范围按固定版本及客户端测试验收；不以自行新增设备登记流程替代上游功能。

## 日历、倍率与换套餐补充

默认工作区 Asia/Shanghai，服务区间左闭右开；月付按锚点日推进，目标月无该日时钳制到月末，但保留原始锚点（1月31日→2月末→3月31日）。当天零点按工作区时区转 UTC，不使用机器本地时区。

中途加入默认按剩余服务秒数比例计费，报价明示起止时刻、分摊方案和 rounding；同一账期整组成本分摊最终通过最大余数法保证总和。退出默认到期停止，不自动计算现金退款；选择立即退出时先生成费用/退款预览并由有权限者确认。换套餐使用新订单及权益版本，旧额度剩余是否折抵由固化的销售政策决定，不能静默丢弃或叠加。

计费倍率存分子/分母；对一个周期的累计物理字节乘倍率，再与上一累计计费值求增量，从而减少每个小样本四舍五入偏差。来源/节点不同倍率必须分开计量，然后聚合。账本记录物理 bytes、billable bytes、倍率版本和计量来源。倍率变更从新权益/新计量边界生效，不能追溯改账。

共享同一用户身份的多个套餐应分配独立接入凭据与范围以保证可归因；合并订阅只合并输出入口，不自动合并所有额度。基础包与加油包合并消耗必须有同 scope 的显式购买政策。外部订阅无法获得逐成员计量时显示“不支持按人计量”，不以外部总量分配到用户。
