<details class="space-top"><summary>行为限速规则格式</summary><p class="hint">留空继承上层规则；enabled=false 明确关闭。成员覆盖套餐与全局，套餐覆盖全局。sustained 使用 durationSeconds 判断连续超速，burst 使用 windowSeconds 和 hits 判断窗口命中。priority 数值较小者优先；处罚到期自动释放。仅使用连续有效的下载采样。</p><pre class="code-editor short">{JSON.stringify({enabled:true,maxGapSeconds:15,rules:[{id:'sustained-download',type:'sustained',thresholdMbps:100,durationSeconds:30,limitMbps:20,penaltySeconds:300,priority:10,notify:true},{id:'burst-download',type:'burst',thresholdMbps:200,windowSeconds:60,hits:3,limitMbps:10,penaltySeconds:120,priority:5,notify:true}]},null,2)}</pre></details>
