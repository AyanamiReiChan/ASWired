<script lang="ts">import {createBehaviorPreset} from '../behavior-presets';</script>
<details class="space-top"><summary>行为限速规则格式</summary><p class="hint">成员或套餐留空继承上层规则，全局留空使用内置均衡规则；enabled=false 明确关闭。成员覆盖套餐与全局，套餐覆盖全局。sustained 使用 durationSeconds 判断连续超速；burst 使用 windowSeconds 和 hits 统计窗口内超速的采样次数，并非独立下载次数。priority 数值较小者优先；处罚到期自动释放。仅使用连续有效的下载采样。下面是默认均衡规则。</p><pre class="code-editor short">{JSON.stringify(createBehaviorPreset(),null,2)}</pre></details>
